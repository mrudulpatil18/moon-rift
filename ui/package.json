{
  "name": "maze-ui",
  "version": "1.0.0",
  "main": "dist/script.js",
  "scripts": {
    "clean": "rm -rf dist/*",
    "build": "npm run clean && tsc && npm run build:cp-public",
    "build:cp-public": "cp -r ./public/* ./dist/",
    "watch": "tsc src/script.ts --watch --outDir ./dist",
    "type": "tsc --noEmit"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
